<style type="text/css">
.sign-in-container {
    max-width: 500px;
}
</style>

<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
   <section class="parallax-section" data-scrollax-parent="true">
                        <div class="bg par-elem "  data-bg="<%= image_path('blog.jpg') %>" data-scrollax="properties: { translateY: '30%' }"></div>
                        <div class="overlay"></div>
                        <div class="container">
                            <div class="section-title center-align">
                                <h2><span>Construye tu mejor versión</span></h2>
                                <div class="breadcrumbs fl-wrap"><a href="#">Blog</a></div>
                                <span class="section-separator"></span>
                            </div>
                        </div>
                       
                    </section>

                     <div class="container sign-in-container">
    <div class="tabs-container">
      <div class="tab">
        <div id="tab-1" class="tab-content">
          <div class="custom-form">

                     <div class="row">
                <div class="col-md-12">
                  <div class="field">
                     <%= form.label :nombre %><br />
                    <%= form.text_field :nombre, id: :blog_nombre %>
                  </div>
                </div>
              </div>

                  <div class="field">
                    <%= form.label :contenido %>
                    <%= form.text_area :contenido, id: :blog_contenido %>
                  </div>


                      <div class="row">
                <div class="col-md-8 col-md-offset-4">
                  <button type="submit" class="log-submit-btn">
                    <span>Crear contenido</span>
                  </button>
                </div>
              </div>
                    <% end %>
                    <br><br>
